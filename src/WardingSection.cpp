
/*
class WardingSection(Line){
private:
    float radius; // the radius of the circle it came from
public:
    WardingSection(std::vector<std::pair<int, int>> data, float radius):Line(), LineData(data){
        self.radius = radius;
    }
    int getIntegrity(){
        // figure out how disfigured it is based on how it should be, 
        //  quantify that, and return that number.
    }
}
*/